{
  "ignore": ["**/*.test.ts", "**/*.spec.ts", "node_modules", "client", "build"],
  "watch": ["server"],
  "exec": "ts-node -P ./server/tsconfig.json ./server/index.ts",
  "ext": "ts",
  "env": {
    "NODE_ENV": "development"
  }
}
